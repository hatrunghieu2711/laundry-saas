# Secrets — KHÔNG BAO GIỜ commit
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
.coverage.*

# IDE / OS
.idea/
.vscode/
.DS_Store

# Docker volumes (nếu mount cục bộ)
pg_data/
redis_data/

# Uploads runtime (logo phiếu in) — file người dùng tải lên, KHÔNG commit
uploads/

# Database backups — KHÔNG BAO GIỜ commit (chứa data thật)
*.sql
*.sql.gz
backups/
scripts/rls/*.sql
.env.bak
