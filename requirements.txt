# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Database (SQLAlchemy 2.0 async + asyncpg + Alembic)
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Cache / rate limit
redis==5.2.1

# HTTP client (Telegram API; cũng dùng làm test client)
httpx==0.28.1

# Config / validation (Pydantic v2)
pydantic==2.10.4
pydantic-settings==2.7.1

# Upload logo phiếu in (multipart form + xử lý ảnh resize/optimize)
python-multipart==0.0.20
pillow==11.1.0

# Auth (JWT + bcrypt)
pyjwt==2.10.1
passlib==1.7.4
# 4.0.1 giữ bcrypt.__about__ mà passlib 1.7.4 đọc — tránh warning ồn ở log.
bcrypt==4.0.1

# Test
pytest==8.3.4
pytest-asyncio==0.25.0
